dashboard_index:
    path: /dashboard
    defaults: { _controller: tickit.controller.default:defaultAction }
    methods: [GET]
    options:
        expose: true

index:
    path: /
    defaults: { _controller: tickit.controller.default:defaultAction }
    methods: [GET]
    options:
        expose: true
