dashboard_index:
    pattern: /dashboard
    defaults: { _controller: tickit.controller.default:defaultAction }
    options:
        expose: true

index:
    pattern: /
    defaults: { _controller: tickit.controller.default:defaultAction }
    options:
        expose: true
