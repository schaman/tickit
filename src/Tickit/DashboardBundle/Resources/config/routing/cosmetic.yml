dashboard_index:
    pattern: /dashboard
    defaults: { _controller: TickitCoreBundle:Default:default }
    options:
        expose: true

index:
    pattern: /
    defaults: { _controller: TickitCoreBundle:Default:default }
    options:
        expose: true
