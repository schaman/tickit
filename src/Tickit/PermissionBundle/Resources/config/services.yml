services:
  permissions:
    class: Tickit\PermissionBundle\Service\PermissionService
    arguments: [@session, @tickit_cache.factory]
  permissions_sync:
      class: Tickit\PermissionBundle\Listener\Sync
      arguments: [@security.context, @permissions, @doctrine, @tickit_cache.factory]
      tags:
        - { name: kernel.event_listener, event: kernel.controller, method: onCoreController }